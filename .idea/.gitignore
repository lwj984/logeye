### 操作系统生成文件 ###
.DS_Store            # macOS 系统文件
Thumbs.db            # Windows 缩略图缓存
ehthumbs.db
Desktop.ini

### 依赖目录 ###
node_modules/        # 最重要的！不要提交依赖
package-lock.json    # 需要锁文件但不要提交（建议用 npm-shrinkwrap.json 替代）
npm-debug.log*       # npm 调试日志
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

### Electron 构建产物 ###
dist/                # 打包输出目录
release/             # 生成的可执行文件
out/                 # 编译输出目录（如 TypeScript 编译结果）
build/               # 构建中间文件

### 应用缓存和临时文件 ###
/renderer-process/__pycache__/  # Python 缓存
/.vscode/                       # IDE 配置（建议团队共享配置单独提交）
/.idea/                         # JetBrains IDE 配置
*.log                           # 应用日志文件
*.tmp                           # 临时文件
*.pid                           # 进程 ID 文件

### 环境变量和敏感数据 ###
.env                # 永远不要提交环境变量！
.env.local          # 本地环境变量
.env.*.local        # 环境特定的本地变量
secrets.json        # 包含敏感信息的配置文件
*.key               # 密钥文件
*.pem
*.crt

### 打包相关 ###
app.asar            # 打包后的应用
app/                # 如果使用了 asar 解压目录
*.dmg               # macOS 安装包
*.exe               # Windows 安装包
*.deb               # Linux deb 包
*.rpm               # Linux rpm 包
*.snap              # Linux snap 包
*.AppImage          # Linux AppImage
*.zip               # 压缩包文件

### 调试文件 ###
*.map               # Source map 文件
coverage/           # 测试覆盖率报告

### 开发工具相关 ###
.electron-build/     # electron-builder 缓存
.release/            # electron-release 工具生成
electron-download/   # Electron 二进制下载缓存

### 编辑器配置 ###
.vscode/*            # VSCode 配置（除了需共享的设置）
!.vscode/settings.json # 建议单独管理共享配置
!.vscode/extensions.json
*.code-workspace     # VSCode 工作区文件（建议本地使用）

### 系统无关通用忽略 ###
.cache/             # 编译缓存（如 webpack）
.temp/              # 临时文件目录
/logs/              # 日志目录